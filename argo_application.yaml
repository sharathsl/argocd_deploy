apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argo-app-dev
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/sharathsl/argocd_deploy.git
    targetRevision: dev
    path: dev
  destination:
    server: https://kubernetes.default.svc
    namespace: dev

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true
